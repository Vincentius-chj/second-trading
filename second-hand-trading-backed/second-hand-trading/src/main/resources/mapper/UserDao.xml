<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.second.hand.trading.dao.UserDao">
    <resultMap id="BaseResultMap" type="com.second.hand.trading.model.UserModel">
        <id column="id" jdbcType="BIGINT" property="id"/>
        <result column="account_number" jdbcType="VARCHAR" property="accountNumber"/>
        <result column="user_password" jdbcType="VARCHAR" property="userPassword"/>
        <result column="nickname" jdbcType="VARCHAR" property="nickname"/>
        <result column="avatar" jdbcType="VARCHAR" property="avatar"/>
        <result column="background_img" jdbcType="VARCHAR" property="backgroundImg"/>
        <result column="sign_in_time" jdbcType="TIMESTAMP" property="signInTime"/>
        <result column="user_status" jdbcType="TINYINT" property="userStatus"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, account_number, nickname, avatar, background_img, sign_in_time, user_status
    </sql>

    <select id="userLogin" resultMap="BaseResultMap">
        select id, account_number, nickname, avatar, sign_in_time, user_status
        from second_hand_trading.sh_user
        where account_number = #{accountNumber}
          and user_password = #{userPassword}
    </select>
    <select id="selectByPrimaryKey" parameterType="java.lang.Long" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from second_hand_trading.sh_user
        where id = #{id,jdbcType=BIGINT}
    </select>

    <!--先添加的-->
    <!--    <select id="selectByUserName" parameterType="java.lang.String" resultMap="Long">
        select id from sh_user where nickname = #{nickname,jdbcType=VARCHAR};
      </select>-->

    <select id="countNormalUser" resultType="int">
        select count(*)
        from second_hand_trading.sh_user
        where user_status is null
           or user_status = 0
    </select>

    <select id="countBanUser" resultType="int">
        select count(*)
        from second_hand_trading.sh_user
        where user_status = 1
    </select>

    <select id="getUserList" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from second_hand_trading.sh_user
    </select>

    <select id="getUserByNumber" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from second_hand_trading.sh_user where account_number = #{searchValue} and user_status = #{mode}
    </select>

    <select id="getUserByMultiFields" parameterType="map" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from second_hand_trading.sh_user
        <where>
            <if test="userStatus != null and userStatus != 0">
                and user_status = #{userStatus}
            </if>
            <if test="userStatus != null and userStatus == 0">
                and (user_status is null or user_status = 0)
            </if>
            <if test="id != null and id != ''">
                and id = #{id}
            </if>
            <if test="nickname != null and nickname != ''">
                and nickname LIKE CONCAT('%', #{nickname}, '%')
            </if>
            <if test="accountNumber != null and accountNumber != ''">
                and account_number LIKE CONCAT('%', #{accountNumber}, '%')
            </if>
            <if test="startTime != null and startTime != '' and endTime != null and endTime != ''">
                and DATE(sign_in_time) BETWEEN #{startTime} AND #{endTime}
            </if>
            <if test="startTime != null and startTime != '' and (endTime == null or endTime == '')">
                and DATE(sign_in_time) >= #{startTime}
            </if>
            <if test="endTime != null and endTime != '' and (startTime == null or startTime == '')">
                and DATE(sign_in_time) &lt;= #{endTime}
            </if>
        </where>
        order by id desc
        limit #{begin}, #{nums}
    </select>

    <select id="countUserByMultiFields" resultType="int">
        select COUNT(*)
        from second_hand_trading.sh_user
        <where>
            <if test="userStatus != null and userStatus != 0">
                and user_status = #{userStatus}
            </if>
            <if test="userStatus != null and userStatus == 0">
                and (user_status is null or user_status = 0)
            </if>
            <if test="id != null and id != ''">
                and id = #{id}
            </if>
            <if test="nickname != null and nickname != ''">
                and nickname LIKE CONCAT('%', #{nickname}, '%')
            </if>
            <if test="accountNumber != null and accountNumber != ''">
                and account_number LIKE CONCAT('%', #{accountNumber}, '%')
            </if>
            <if test="startTime != null and startTime != '' and endTime != null and endTime != ''">
                and DATE(sign_in_time) BETWEEN #{startTime} AND #{endTime}
            </if>
            <if test="startTime != null and startTime != '' and (endTime == null or endTime == '')">
                and DATE(sign_in_time) >= #{startTime}
            </if>
            <if test="endTime != null and endTime != '' and (startTime == null or startTime == '')">
                and DATE(sign_in_time) &lt;= #{endTime}
            </if>
        </where>
    </select>

    <select id="getNormalUser" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from second_hand_trading.sh_user where user_status is null or user_status = 0 order by id desc limit #{begin},
        #{nums}
    </select>

    <select id="getBanUser" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from second_hand_trading.sh_user where user_status = 1 order by id desc limit #{begin}, #{nums}
    </select>

    <select id="findUserByList" resultMap="BaseResultMap">
        select id,nickname, avatar, sign_in_time
        from second_hand_trading.sh_user where id in
        <foreach collection="idList" index="index" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <delete id="deleteByPrimaryKey" parameterType="java.lang.Long">
        delete
        from second_hand_trading.sh_user
        where id = #{id,jdbcType=BIGINT}
    </delete>

    <insert id="insert" keyColumn="id" keyProperty="id" parameterType="com.second.hand.trading.model.UserModel"
            useGeneratedKeys="true">
        insert into second_hand_trading.sh_user (account_number, user_password, nickname,
                                                 avatar, sign_in_time)
        values (#{accountNumber,jdbcType=VARCHAR}, #{userPassword,jdbcType=VARCHAR}, #{nickname,jdbcType=VARCHAR},
                #{avatar,jdbcType=VARCHAR}, #{signInTime,jdbcType=TIMESTAMP})
    </insert>

    <insert id="insertSelective" keyColumn="id" keyProperty="id"
            parameterType="com.second.hand.trading.model.UserModel" useGeneratedKeys="true">
        insert into second_hand_trading.sh_user
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="accountNumber != null">
                account_number,
            </if>
            <if test="userPassword != null">
                user_password,
            </if>
            <if test="nickname != null">
                nickname,
            </if>
            <if test="avatar != null">
                avatar,
            </if>
            <if test="signInTime != null">
                sign_in_time,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="accountNumber != null">
                #{accountNumber,jdbcType=VARCHAR},
            </if>
            <if test="userPassword != null">
                #{userPassword,jdbcType=VARCHAR},
            </if>
            <if test="nickname != null">
                #{nickname,jdbcType=VARCHAR},
            </if>
            <if test="avatar != null">
                #{avatar,jdbcType=VARCHAR},
            </if>
            <if test="signInTime != null">
                #{signInTime,jdbcType=TIMESTAMP},
            </if>
        </trim>
    </insert>

    <update id="updateByPrimaryKeySelective" parameterType="com.second.hand.trading.model.UserModel">
        update second_hand_trading.sh_user
        <set>
            <if test="accountNumber != null">
                account_number = #{accountNumber,jdbcType=VARCHAR},
            </if>
            <if test="userPassword != null">
                user_password = #{userPassword,jdbcType=VARCHAR},
            </if>
            <if test="nickname != null">
                nickname = #{nickname,jdbcType=VARCHAR},
            </if>
            <if test="avatar != null">
                avatar = #{avatar,jdbcType=VARCHAR},
            </if>
            <if test="backgroundImg != null">
                background_img = #{backgroundImg,jdbcType=VARCHAR},
            </if>
            <if test="signInTime != null">
                sign_in_time = #{signInTime,jdbcType=TIMESTAMP},
            </if>
            <if test="userStatus != null">
                user_status = #{userStatus},
            </if>
        </set>
        where id = #{id,jdbcType=BIGINT}
    </update>

    <update id="updateByPrimaryKey" parameterType="com.second.hand.trading.model.UserModel">
        update second_hand_trading.sh_user
        set account_number = #{accountNumber,jdbcType=VARCHAR},
            user_password  = #{userPassword,jdbcType=VARCHAR},
            nickname       = #{nickname,jdbcType=VARCHAR},
            avatar         = #{avatar,jdbcType=VARCHAR},
            background_img = #{backgroundImg,jdbcType=VARCHAR},
            sign_in_time   = #{signInTime,jdbcType=TIMESTAMP}
        where id = #{id,jdbcType=BIGINT}
    </update>

    <update id="updatePassword">
        <!--userSignIn-->
        update second_hand_trading.sh_user
        set user_password = #{newPassword,jdbcType=VARCHAR}
        where id = #{id,jdbcType=BIGINT}
        and user_password = #{oldPassword,jdbcType=VARCHAR}
    </update>

    <select id="selectByUserName" resultType="java.lang.Long">
        select id
        from second_hand_trading.sh_user
        where nickname = #{nickname}
    </select>

    <update id="updateUserBackground">
        update second_hand_trading.sh_user
        set background_img = #{backgroundImg,jdbcType=VARCHAR}
        where id = #{id,jdbcType=BIGINT}
    </update>
</mapper>